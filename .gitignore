# 依赖
node_modules/

# 构建产物：按 Obsidian 官方模板约定，main.js 不进仓库，只作为 GitHub Release 附件分发
main.js
*.map

# 插件在 vault 里生成的本地数据
data.json

# 打包中间产物（由 npm run release 生成）
release/
code-block-auto-collapse-*.zip
code-block-auto-collapse-*/

# 本地回归测试用的 vault（官方社区目录的扫描器也会跳过这些名字）
.obsidian/
test-vault/

# 参考实现：需要时临时克隆，见 tools/generate-character-weights.mjs
_CodeGlancePro/

# 本地工作目录：不进仓库的参考资料与工作文档。
# AGENTS.md / 你需要做的事.md / PLUGIN_SUBMISSION_ZH.md / ISSUE_DRAFTS.md / _ref/ 都收在里面，
# 分工说明见 _local/README.md。之所以整个目录一起忽略，是为了让仓库根目录只剩该进仓库的文件。
_local/

# 兜底：这些名字若出现在别处同样不该提交
_ref/
AGENTS.md
HANDOVER.md
你需要做的事.md
PLUGIN_DEVELOPMENT.md
PLUGIN_SUBMISSION_ZH.md

# 本地工作目录：解压出来的发布包副本、旧版本备份
backup/

# 本地助手的工作记忆，不属于插件源码
.workbuddy-ai/

# 编辑器与操作系统噪声
.vscode/
.idea/
*.iml
.DS_Store
Thumbs.db
desktop.ini

# 日志与缓存
*.log
npm-debug.log*
.eslintcache
