# 依赖
node_modules/

# 构建产物：按 Obsidian 官方模板约定，main.js 不进仓库，只作为 GitHub Release 附件分发
main.js
*.map

# 插件在 vault 里生成的本地数据
data.json

# 打包中间产物（由 npm run release 生成）
release/
code-block-auto-collapse-*.zip
code-block-auto-collapse-*/

# 本地回归测试用的 vault（官方社区目录的扫描器也会跳过这些名字）
.obsidian/
test-vault/

# 参考实现：需要时临时克隆，见 tools/generate-character-weights.mjs
_CodeGlancePro/

# 本地的参考资料目录，不是插件源码
_ref/

# 官方文档的中文整理稿（本地参考用，非插件源码；内容版权属 Obsidian）
PLUGIN_SUBMISSION_ZH.md

# 本地工作目录：解压出来的发布包副本、旧版本备份
backup/

# 本地助手的工作记忆，不属于插件源码
.workbuddy-ai/

# 给 AI 编码助手看的项目导览（本地工作交接说明，不进仓库、不随插件发布）
AGENTS.md

# 维护者交接说明：上架待办、维护与优化方向（面向自己，不进仓库）
HANDOVER.md

# 编辑器与操作系统噪声
.vscode/
.idea/
*.iml
.DS_Store
Thumbs.db
desktop.ini

# 日志与缓存
*.log
npm-debug.log*
.eslintcache
